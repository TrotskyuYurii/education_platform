/**
 * Єдине джерело правди для промпту та еталонного шаблону інструкції.
 * Використовується і сервером (автоматичний аналіз документа через ШІ),
 * і адмінкою (кнопка «Скопіювати промпт» для ChatGPT / Gemini / DeepSeek),
 * щоб правила в обох місцях не розходилися.
 */

export interface AvailableImage {
  /** Ім'я файлу в теці assets/, наприклад img-001.png */
  fileName: string;
  page?: number;
  width?: number;
  height?: number;
}

/**
 * Правила щодо зображень. Головне: жодного base64 — платформа зберігає скріншоти
 * окремими файлами у теці документа, а Markdown лише посилається на них.
 */
export const IMAGE_RULES = `ПРАВИЛА ЩОДО ЗОБРАЖЕНЬ (ОБОВ'ЯЗКОВІ):
1. КАТЕГОРИЧНО ЗАБОРОНЕНО вбудовувати зображення у файл кодом Base64 (\`data:image/...;base64,...\`) — такі вставки система видалить, а документ стане неповним.
2. Скріншоти та схеми з оригіналу вже збережені платформою окремими файлами у теці \`assets/\` поруч із .md файлом. Посилайся лише на них.
3. Формат посилання — відносний шлях без домену: \`![Змістовний підпис до скріншота](assets/img-001.png)\`.
4. Дозволено використовувати ВИКЛЮЧНО ті імена файлів, що перелічені нижче у блоці «ДОСТУПНІ ЗОБРАЖЕННЯ». Не вигадуй власних імен і не посилайся на зовнішні сайти — посилання на неіснуючий файл буде прибране разом із підписом.
5. Постав кожне зображення точно в те місце тексту або в той крок, якого воно стосується (орієнтир — номер сторінки біля імені файлу). Одне зображення використовується один раз.
6. Якщо зображення не стосується жодного кроку чи розділу — просто не вставляй його.`;

/** Блок із переліком файлів, які модель має право використати. */
export function buildAvailableImagesBlock(images: AvailableImage[] = []): string {
  if (images.length === 0) {
    return `ДОСТУПНІ ЗОБРАЖЕННЯ: немає.
У цьому документі не знайдено придатних скріншотів, тому НЕ вставляй у Markdown жодного посилання на зображення.`;
  }

  const lines = images.map(img => {
    const details = [
      img.page ? `сторінка ${img.page}` : null,
      img.width && img.height ? `${img.width}×${img.height} px` : null
    ].filter(Boolean).join(', ');
    return `- assets/${img.fileName}${details ? ` — ${details}` : ''}`;
  });

  return `ДОСТУПНІ ЗОБРАЖЕННЯ (вже збережені платформою у теці assets/, порядок відповідає порядку появи в документі):
${lines.join('\n')}`;
}

/** Основне тіло промпту без блоку з переліком зображень. */
export const INSTRUCTION_PROMPT_BODY = `Ти — провідний експерт з корпоративного навчання, регламентів бізнес-процесів та укладання професійних тестів.
Твоє завдання: перенести додану робочу інструкцію/регламент компанії у цей додаток У ТОМУ САМОМУ ВИГЛЯДІ (повний текст, розділи, таблиці, малюнки/скріншоти), забезпечити співробітнику зручне повноцінне читання та ознайомлення, а в кінці вивести контрольні блоки та тестові питання для квіз-опитування.

Формат результату — єдиний файл Markdown (.md) суворо за такою структурою:

# Назва інструкції: [Повна назва інструкції або регламенту]
**Підзаголовок:** [Коротке роз'яснення для кого і в яких ситуаціях застосовується]
**Підрозділ:** [Назва підрозділу, наприклад: Відділ роздрібного продажу / Казначейство / Бухгалтерія / Склад]
**Суть:** [1-2 речення з головною суттю регламенту — що потрібно знати в першу чергу]
**Роль:** [Одне зі значень: all | cashier | manager | accountant]
**Першоджерело:** [Номер наказу, регламенту або номер сторінки, наприклад: Стор. 1–4, Регламент №12]
**Час читання:** [Орієнтовний час вивчення, наприклад: 5 хв]

### ПОВНИЙ ТЕКСТ ІНСТРУКЦІЇ
[Встав сюди ПОВНИЙ оригінальний текст регламенту без скорочень!
Зберігай усю початкову структуру, заголовки, параграфи, виноски, примітки та описи.
Ілюстрації, схеми та скріншоти вікон програми став у відповідні місця тексту посиланням на файл:
![Підпис до зображення](assets/img-001.png)]

### ПОКРОКОВИЙ ПОРЯДОК ДІЙ
[Якщо регламент містить послідовність операцій, розпиши їх покроково:]
#### Крок 1: [Коротка назва дії]
[Детальний опис дій співробітника в інтерфейсі програми чи на робочому місці]
![Скріншот до кроку](assets/img-002.png)
💡 Підказка: [Корисна порада для прискорення роботи або запобігання помилкам]
⚠️ Увага: [Попередження про критичні нюанси]

#### Крок 2: [Наступна дія]
[Опис кроку 2]

---
В КІНЦІ ОСНОВНОЇ ІНСТРУКЦІЇ ОБОВ'ЯЗКОВО СФОРМУЙ АНАЛІТИЧНІ БЛОКИ ТА СИСТЕМНІ ДІЇ:

### ОСНОВНІ ВИСНОВКИ
- [Ключовий висновок 1 — головне правило, яке працівник повинен запам'ятати]
- [Ключовий висновок 2]
- [Ключовий висновок 3]

### КЛЮЧОВІ ПОЛЯ ТА РЕКВІЗИТИ
- [Обов'язкове поле/реквізит 1: наприклад, «Статус чека — тільки "Пробитий"»]
- [Обов'язкове поле/реквізит 2: наприклад, «Номер первинного фіскального чека»]
- [Обов'язкове поле/реквізит 3: наприклад, «Заява покупця з паспортними даними при сумі > 100 грн»]

### СТОП-СПИСКИ
- [Критична заборона 1: Категорично заборонено видавати готівку, якщо покупка була оплачена карткою!]
- [Критична заборона 2: Заборонено проводити повернення без заяви покупця при сумі понад 100 грн!]
- [Критична заборона 3: Дія, яка тягне за собою збій, штраф чи скаргу клієнта]

### АВТОМАТИЧНІ ДІЇ СИСТЕМИ
- [Дія 1: Що облікова програма (BAS, CRM, ПРРО) проводить автоматично]
- [Дія 2: Автоматичні бухгалтерські, касові чи складські рухи]

### ТАБЛИЦЯ ВІДПОВІДНОСТЕЙ ТА ВІДПОВІДАЛЬНОСТІ
| Ситуація / Умова | Дія співробітника | Відповідальна особа | Термін |
| --- | --- | --- | --- |
| [Умова 1] | [Дія 1] | [Посада] | [Термін] |
| [Умова 2] | [Дія 2] | [Посада] | [Термін] |

---
БЛОК ПИТАНЬ ДЛЯ КВІЗ-ОПИТУ (ТЕСТУВАННЯ):

### ПИТАННЯ: [Текст практичного запитання 1 на основі реальної робочої ситуації?]
**Складність:** [easy | medium | hard]
**Контекст:** [Реальна робоча ситуація клієнта або інцидент, на якому ґрунтується питання]
**Першоджерело:** [Пункт регламенту чи сторінка]
- [ ] [Неправильний варіант відповіді A]
- [x] [ПРАВИЛЬНИЙ варіант відповіді — позначається строго через [x]]
- [ ] [Неправильний варіант відповіді B]
- [ ] [Неправильний варіант відповіді C]
**Пояснення:** [Детальне обґрунтування, чому ця відповідь правильна з посиланням на регламент і логіку системи]

ВИМОГИ ДО ТЕСТОВИХ ПИТАНЬ:
1. Склади від 3 до 6 якісних запитань різної складності (easy, medium, hard).
2. Запитання обов'язково повинні спиратися на текст інструкції, Ключові поля та СТОП-СПИСКИ.
3. Рівно один варіант відповіді має бути позначений як правильний через [x].
4. У відповіді виводь виключно готовий текст Markdown без вступних слів, привітань та сторонніх коментарів.`;

/**
 * Збирає промпт для автоматичного аналізу документа.
 * `images` — скріншоти, які платформа вже витягнула з оригіналу та зберегла у файли.
 */
export function buildInstructionPrompt(
  images: AvailableImage[] = [],
  options: { inlineMarkers?: boolean } = {}
): string {
  // Для .docx платформа віддає моделі текст, у якому посилання на скріншоти вже
  // стоять на своїх місцях, — тоді розставляти їх наново не треба, лише зберегти.
  const placement = options.inlineMarkers && images.length > 0
    ? '\nУ доданому тексті посилання на ці файли вже стоять на своїх місцях (так вони розташовані в оригіналі).\n'
      + 'Перенеси кожне з них у Markdown у тому ж місці, замінивши на `![Змістовний підпис](assets/...)`.\n'
      + 'Не переставляй зображення між кроками і не вигадуй для них нових місць.'
    : '';

  return [
    INSTRUCTION_PROMPT_BODY,
    '---',
    IMAGE_RULES,
    '',
    buildAvailableImagesBlock(images) + placement
  ].join('\n');
}

/**
 * Промпт для ручного використання в інших моделях (ChatGPT, Gemini, DeepSeek).
 * Список конкретних файлів там невідомий, тому пояснюємо загальне правило іменування.
 */
export const AI_PROMPT_GUIDE = `ВИКОРИСТОВУЙ ЦЕЙ ПРОМПТ ДЛЯ ІНШИХ МОДЕЛЕЙ ШІ (ChatGPT, Claude, Gemini, DeepSeek):
-------------------------------------------------------------------------
${INSTRUCTION_PROMPT_BODY}

---
${IMAGE_RULES}

ЯКЩО ТИ ГОТУЄШ ФАЙЛ ВРУЧНУ (без автоматичного аналізу платформою):
- Збережи скріншоти з регламенту окремими файлами та назви їх послідовно: img-001.png, img-002.png, …
- У Markdown посилайся на них як assets/img-001.png.
- Під час імпорту .md прикріпіть ці файли разом з оригіналом документа — платформа збереже їх у теці інструкції.
- Якщо у вашому .md все ж лишилися вставки Base64, платформа автоматично винесе їх в окремі файли під час імпорту.`;
